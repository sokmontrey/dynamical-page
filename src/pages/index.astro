<canvas id="canvas"></canvas>

<script>
    import { Renderer, Rectangle, Container } from 'dynamicaljs';

    const canvas = document.getElementById('canvas');
    const renderer = new Renderer(canvas);

    // renderer.update((delta_time)=>{
    //     renderer.clear();
    // });
</script>

